import React from 'react';
import './style.scss'

const FileInput = ({ label, error, ...rest }) => {
    return (
        <div className="component-file-input">

        </div>
    )
}

export default FileInput