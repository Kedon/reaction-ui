import React from 'react';
import './style.scss'

const Calendar = ({ label, error, ...rest }) => {
    return (
        <div className="component-calendar">

        </div>
    )
}

export default Calendar