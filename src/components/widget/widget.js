import React from 'react';
import './widget.scss'

const Widget = ({ label, error, ...rest }) => {
    return (
        <div className="component-widget">

        </div>
    )
}

export default Widget